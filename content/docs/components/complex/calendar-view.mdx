---
title: "Calendar View"
description: "Full-featured calendar for displaying and managing events in month, week, or day views"
---

import { ComponentDemo, DemoGrid } from '@/app/components/ComponentDemo';

The Calendar View component provides an Airtable-style calendar for visualizing records as events with support for different view modes.

## Basic Usage

<ComponentDemo
  schema={{
    type: 'calendar-view',
    view: 'month',
    data: [
      {
        id: 1,
        title: 'Team Meeting',
        start: new Date(new Date().setHours(10, 0, 0, 0)).toISOString(),
        end: new Date(new Date().setHours(11, 0, 0, 0)).toISOString(),
        color: '#3b82f6',
        allDay: false
      },
      {
        id: 2,
        title: 'Project Deadline',
        start: new Date(new Date().setDate(new Date().getDate() + 3)).toISOString(),
        color: '#ef4444',
        allDay: true
      },
      {
        id: 3,
        title: 'Conference',
        start: new Date(new Date().setDate(new Date().getDate() + 7)).toISOString(),
        end: new Date(new Date().setDate(new Date().getDate() + 9)).toISOString(),
        color: '#10b981',
        allDay: true
      }
    ],
    className: 'h-[600px] border rounded-lg'
  }}
  title="Event Calendar"
/>

## Schema

```typescript
interface CalendarEvent {
  id: string | number;                   // Unique event identifier
  title: string;                         // Event title
  start: Date | string;                  // Event start date/time
  end?: Date | string;                   // Event end date/time (optional)
  allDay?: boolean;                      // All-day event flag
  color?: string;                        // Event color
  data?: any;                            // Additional event data
}

interface CalendarViewSchema {
  type: 'calendar-view';
  
  // Data
  data: Record<string, any>[];          // Records to display as events
  
  // Field mapping (map record fields to event properties)
  titleField?: string;                   // Field for event title (default: 'title')
  startDateField?: string;               // Field for start date (default: 'start')
  endDateField?: string;                 // Field for end date (default: 'end')
  allDayField?: string;                  // Field for all-day flag (default: 'allDay')
  colorField?: string;                   // Field for color (default: 'color')
  
  // Color mapping
  colorMapping?: Record<string, string>; // Map field values to colors
  
  // View configuration
  view?: 'month' | 'week' | 'day';      // Calendar view mode (default: 'month')
  currentDate?: string | Date;           // Initial calendar date
  
  // Features
  allowCreate?: boolean;                 // Allow creating events by clicking dates
  
  // Events
  onEventClick?: (event: any) => void;  // Event click handler
  onDateClick?: (date: Date) => void;   // Date click handler
  onViewChange?: (view: string) => void; // View change handler
  onNavigate?: (date: Date) => void;    // Navigation handler
  
  // Styling
  className?: string;                    // Tailwind CSS classes
  
  // Base properties
  id?: string;
  visible?: boolean | string;
  testId?: string;
}
```

## Examples

### Project Timeline

<ComponentDemo
  schema={{
    type: 'calendar-view',
    view: 'month',
    titleField: 'name',
    startDateField: 'startDate',
    endDateField: 'endDate',
    colorField: 'status',
    colorMapping: {
      'In Progress': '#3b82f6',
      'Completed': '#10b981',
      'Planning': '#f59e0b',
      'Blocked': '#ef4444'
    },
    data: [
      {
        id: 1,
        name: 'Website Redesign',
        startDate: new Date(2024, 0, 15).toISOString(),
        endDate: new Date(2024, 0, 25).toISOString(),
        status: 'In Progress',
        allDay: true
      },
      {
        id: 2,
        name: 'Mobile App Launch',
        startDate: new Date(2024, 0, 20).toISOString(),
        endDate: new Date(2024, 0, 22).toISOString(),
        status: 'Planning',
        allDay: true
      },
      {
        id: 3,
        name: 'API Integration',
        startDate: new Date(2024, 0, 10).toISOString(),
        endDate: new Date(2024, 0, 12).toISOString(),
        status: 'Completed',
        allDay: true
      }
    ],
    className: 'h-[600px] border rounded-lg'
  }}
  title="Project Calendar"
/>

### Employee Schedule

<ComponentDemo
  schema={{
    type: 'calendar-view',
    view: 'week',
    titleField: 'employee',
    startDateField: 'shiftStart',
    endDateField: 'shiftEnd',
    colorField: 'department',
    colorMapping: {
      'Sales': '#3b82f6',
      'Support': '#10b981',
      'Engineering': '#8b5cf6',
      'Marketing': '#f59e0b'
    },
    data: [
      {
        id: 1,
        employee: 'John - Morning Shift',
        shiftStart: new Date(new Date().setHours(9, 0, 0, 0)).toISOString(),
        shiftEnd: new Date(new Date().setHours(17, 0, 0, 0)).toISOString(),
        department: 'Engineering',
        allDay: false
      },
      {
        id: 2,
        employee: 'Sarah - Afternoon',
        shiftStart: new Date(new Date().setHours(13, 0, 0, 0)).toISOString(),
        shiftEnd: new Date(new Date().setHours(21, 0, 0, 0)).toISOString(),
        department: 'Support',
        allDay: false
      },
      {
        id: 3,
        employee: 'Mike - Full Day',
        shiftStart: new Date(new Date().setDate(new Date().getDate() + 1)).toISOString(),
        shiftEnd: new Date(new Date().setDate(new Date().getDate() + 1)).toISOString(),
        department: 'Sales',
        allDay: true
      }
    ],
    className: 'h-[600px] border rounded-lg'
  }}
  title="Work Schedule"
/>

### Marketing Campaign

<ComponentDemo
  schema={{
    type: 'calendar-view',
    view: 'month',
    titleField: 'campaign',
    startDateField: 'launchDate',
    endDateField: 'endDate',
    colorField: 'type',
    colorMapping: {
      'Social Media': '#1da1f2',
      'Email': '#ea4335',
      'Content': '#34a853',
      'Event': '#fbbc04'
    },
    data: [
      {
        id: 1,
        campaign: 'Summer Sale Launch',
        launchDate: new Date(2024, 0, 18).toISOString(),
        endDate: new Date(2024, 0, 25).toISOString(),
        type: 'Social Media',
        allDay: true
      },
      {
        id: 2,
        campaign: 'Newsletter Q1',
        launchDate: new Date(2024, 0, 22).toISOString(),
        type: 'Email',
        allDay: true
      },
      {
        id: 3,
        campaign: 'Product Webinar',
        launchDate: new Date(2024, 0, 28).toISOString(),
        type: 'Event',
        allDay: false
      }
    ],
    className: 'h-[600px] border rounded-lg'
  }}
  title="Campaign Calendar"
/>

## Features

- **Multiple Views**: Switch between month, week, and day views
- **Event Creation**: Click dates to create new events (when enabled)
- **Event Interaction**: Click events to view details or trigger actions
- **Color Coding**: Automatically map data fields to event colors
- **Field Mapping**: Flexible mapping of your data to calendar events
- **Responsive**: Adapts to different screen sizes
