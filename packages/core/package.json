{
  "name": "@object-ui/core",
  "version": "0.1.0",
  "description": "The Brain. Schema definitions, renderer registry, and logic.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
  }
}
